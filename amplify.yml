version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20 || true
        - npm ci --no-audit --no-fund
    build:
      commands:
        - env | grep -e NEXTAUTH_SECRET -e NEXTAUTH_URL -e ADMIN_PASSWORD >> .env.production
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - ~/.npm/**/*
